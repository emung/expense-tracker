/** Route paths, kept out of `router.tsx` so navigation helpers can use them without importing the router. */
export const paths = {
  expenses: '/cheltuieli',
  report: '/raport',
  settings: '/setari',
} as const;
